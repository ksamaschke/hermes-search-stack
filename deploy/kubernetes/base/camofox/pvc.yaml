apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: camofox-profiles
  labels:
    app.kubernetes.io/name: camofox
    app.kubernetes.io/part-of: hermes-search-stack
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
