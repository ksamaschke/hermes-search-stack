# Backward-compatible alias for deployments that already reference the former
# generic `sandboxed` path. New deployments should select `../gvisor` or
# `../kata` explicitly so runtime-specific compatibility logic cannot leak
# across runtimes.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../gvisor
